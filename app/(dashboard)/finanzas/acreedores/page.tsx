import { AcreedoresPanel } from '@/components/finanzas/acreedores-panel'

// Cuenta corriente de acreedores: cuánto se le debe a cada persona con la que hay cuenta abierta.
export default function AcreedoresPage() {
  return <AcreedoresPanel />
}
